<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_move_inherited_add_fields" model="ir.ui.view">
        <field name="name">account.move.inherited.form.add.fields</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='team_id']" position="after">
                <field name="no_invoice" attrs="{'readonly': [('state', '!=', 'draft')]}" groups="smartest_no_invoice.group_not_declared"/>
            </xpath>
        </field>
    </record>

    <record id="account.action_move_out_invoice_type" model="ir.actions.act_window">
        <field name="domain">[('move_type', '=', 'out_invoice'), ('no_invoice', '=', False)]</field>
    </record>

    <record id="action_move_out_invoice_type_all" model="ir.actions.act_window">
        <field name="name">All Invoices</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="view_id" ref="account.view_out_invoice_tree"/>
        <field name="search_view_id" ref="account.view_account_invoice_filter"/>
        <field name="domain">[('move_type', '=', 'out_invoice')]</field>
        <field name="context">{'default_move_type': 'out_invoice'}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a customer invoice
            </p><p>
                Create invoices, register payments and keep track of the discussions with your customers.
            </p>
        </field>
    </record>
</odoo>