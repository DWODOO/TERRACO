<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record model="ir.ui.view" id="loan_postpone_form">
        <field name="name">hr.loan.postpone.wizard.form</field>
        <field name="model">hr.loan.postpone.wizard</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group string="Loan postpone">
                        <group>
                            <field name="date"/>
                            <field name="amount"/>
                        </group>
                        <group>
                            <field name="new_date"/>
                            <field name="nbr"/>
                        </group>
                    </group>
                    <footer>
                        <button string="Confirm" name="action_confirm" type="object" class="oe_highlight"/>
                        <button string="Cancel" special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>

    <record id="loan_postpone_action" model="ir.actions.act_window">
        <field name="name">Loan refund postpone</field>
        <field name="res_model">hr.loan.postpone.wizard</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="loan_postpone_form"/>
        <field name="target">new</field>
    </record>
</odoo>
