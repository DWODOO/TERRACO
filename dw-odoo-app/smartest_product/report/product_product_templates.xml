<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="report_simple_label_dymo_without_price">
        <div class="o_label_sheet o_label_dymo" t-att-style="padding_page">
            <div class="o_label_full" t-att-style="table_style">
                <div class="text-left o_label_small_barcode">
                    <t t-if="barcode">
                        <!-- `quiet=0` to remove the left and right margins on the barcode -->
                        <div t-out="barcode" style="padding:0"
                             t-options="{'widget': 'barcode', 'quiet': 0, 'symbology': 'auto', 'img_style': barcode_size}"/>
                        <div class="o_label_name" style="line-height: 130%;height:2.0em;background-color: transparent;">
                            <span t-out="barcode"/>
                        </div>
                    </t>
                </div>
                <div class="o_label_name" style="line-height: 130%;height:3.0em;background-color: transparent;">
                    <span t-if="product.is_product_variant" t-field="product.display_name"/>
                    <span t-else="" t-field="product.name"/>
                </div>
                <div class="o_label_left_column">
                    <small class="text-nowrap" t-field="product.default_code"/>
                </div>
                <div class="text-right" style="padding: 0 4px;">
                    <div class="o_label_extra_data">
                        <t t-out="extra_html"/>
                    </div>
                </div>
            </div>
        </div>
    </template>

    <template id="report_productlabel_dymo_without_price">
        <t t-call="web.html_container">
            <t t-set="barcode_size" t-value="'width:45.5mm;height:7.5mm'"/>
            <t t-set="table_style" t-value="'width:100%;height:32mm;'"/>
            <t t-set="padding_page" t-value="'padding: 2mm'"/>
            <t t-foreach="quantity.items()" t-as="barcode_and_qty_by_product">
                <t t-set="product" t-value="barcode_and_qty_by_product[0]"/>
                <t t-foreach="barcode_and_qty_by_product[1]" t-as="barcode_and_qty">
                    <t t-set="barcode" t-value="barcode_and_qty[0]"/>
                    <t t-foreach="range(barcode_and_qty[1])" t-as="qty">
                        <t t-call="smartest_product.report_simple_label_dymo_without_price"/>
                    </t>
                </t>
            </t>
        </t>
    </template>

    <template id="report_productdymo_without_price">
        <t t-call="web.basic_layout">
            <div class="page">
                <t t-call="smartest_product.report_productlabel_dymo_without_price">
                    <t t-set="products" t-value="products"/>
                </t>
            </div>
        </t>
    </template>

    <record id="report_product_template_label_dymo_without_price" model="ir.actions.report">
        <field name="name">Product Label (PDF)</field>
        <field name="model">product.template</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">smartest_product.report_productdymo_without_price</field>
        <field name="report_file">smartest_product.report_productdymo_without_price</field>
        <field name="paperformat_id" ref="product.paperformat_label_sheet_dymo"/>
        <field name="print_report_name">'Products Labels - %s' % (object.name)</field>
        <field name="binding_type">report</field>
    </record>
</odoo>