<?xml version="1.0"?>
<odoo>
    <record id="view_location_security_users_form" model="ir.ui.view">
        <field name="name">location_security.users.form</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook//page[last()]" position="after">
                <page string="Stock">
                    <group string="Locations">
                        <field name="stock_warehouse_ids" widget="many2many_tags"/>
                        <field name="stock_location_ids" widget="many2many_tags"/>
                        <field name="authorized_location_ids" widget="many2many_tags"
                               groups="base.group_no_one" invisible="1"/>
                        <field name="restricted_by_warehouse_location_ids" widget="many2many_tags"
                               groups="base.group_no_one" invisible="1"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
